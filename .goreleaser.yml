version: 2

project_name: kubeadapt-cli

before:
  hooks:
    - go mod tidy

builds:
  - id: default
    main: ./main.go
    binary: kubeadapt-cli
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/kubeadapt/kubeadapt-cli/internal/version.Version={{.Version}}
      - -X github.com/kubeadapt/kubeadapt-cli/internal/version.Commit={{.Commit}}
      - -X github.com/kubeadapt/kubeadapt-cli/internal/version.Date={{.Date}}

archives:
  - id: default
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - "Merge pull request"
      - "Merge branch"

release:
  draft: false
  prerelease: auto

# ──────────────────────────────────────────────────────────────────────────────
# HOMEBREW DISTRIBUTION
# Enables: brew install kubeadapt/tap/kubeadapt-cli
#
# Prerequisite: create github.com/kubeadapt/homebrew-tap (public repo)
# If you don't need Homebrew distribution, delete this entire section.
# ──────────────────────────────────────────────────────────────────────────────
brews:
  - name: kubeadapt-cli
    repository:
      owner: kubeadapt
      name: homebrew-tap
    commit_author:
      name: kubeadapt-platform-assistant[bot]
      email: kubeadapt-platform-assistant[bot]@users.noreply.github.com
    commit_msg_template: "chore(deps): update formula for {{ .ProjectName }} {{ .Tag }}"
    homepage: "https://kubeadapt.io"
    description: "KubeAdapt CLI for Kubernetes cost optimization"
    license: "Apache-2.0"
    install: |
      bin.install "kubeadapt-cli"
    test: |
      system "#{bin}/kubeadapt-cli", "version"

# ──────────────────────────────────────────────────────────────────────────────
# KREW DISTRIBUTION (kubectl plugin manager)
# Enables: kubectl krew install kubeadapt-cli  →  kubectl kubeadapt-cli
#
# Prerequisites:
#   1. Create github.com/kubeadapt/krew-index (public repo)
#   2. For official distribution: open PR to kubernetes-sigs/krew-index
#
# Binary naming: GoReleaser will also produce a kubectl-kubeadapt-cli binary
# that kubectl plugin discovery picks up automatically from PATH.
#
# If this CLI is NOT a kubectl plugin, delete this entire section.
# ──────────────────────────────────────────────────────────────────────────────
# krews:
#   - name: kubeadapt-cli
#     repository:
#       owner: kubeadapt
#       name: krew-index
#       branch: main
#     commit_author:
#       name: platform-assistant[bot]
#       email: platform-assistant[bot]@users.noreply.github.com
#     commit_msg_template: "chore(deps): update krew manifest for {{ .ProjectName }} {{ .Tag }}"
#     homepage: "https://kubeadapt.io"
#     description: "KubeAdapt CLI for Kubernetes cost optimization"
#     short_description: "Kubernetes cost optimization CLI"
